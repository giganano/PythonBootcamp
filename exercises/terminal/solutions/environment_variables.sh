# This is bash code, the same language in your ~/.bash_profile and ~/.bashrc 

# The following will create the environment variable EXAMPLE. After adding this 
# line to your bash_profile and running 'source ~/.bash_profile' (or 
# alternatively your ~/.bashrc) will allow the value of the variable to be 
# printed with 'echo $EXAMPLE'. 

export EXAMPLE="example environment variable" 

# This change will be permanent as long as this line remains in the file you 
# put it into. To get rid of it, simply delete the line and restart your 
# terminal. 
